{% extends 'Admin/layoutAdmin.html.twig' %}

{% block content %}
    <h1>Modifier la catégorie</h1>
    {% if error is not empty %}
        <div class="alert alert-danger">
            <ul>
                <li>{{ error.name }}</li>
            </ul>
        </div>
    {% endif %}
    {% include 'Admin/Category/formupd.html.twig' %}
{% endblock %}

