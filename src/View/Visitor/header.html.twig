<nav class="navbar navbar-expand-lg fixed-top justify-content-between" id="mainNav">
    <div class="container">
        <div class="logo mr mr-5 mt-2 mb-2">
            <a class="navbar-brand js-scroll-trigger" href="/"><img class="img-fluid" src="/assets/images/cestmamaison-1.png" alt="logo"/></a>
        </div>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav text-uppercase  ml-auto">
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="/presentation">Présentation</a>
                </li>
            </ul>
            <div class="dropdown ml-auto">

                <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Catégories
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton2">
                    {% for category in categories %}
                        <a class="dropdown-item" href="/category/{{ category.id }}">{{ category.name }}</a>
                    {% else %}
                        <a class="dropdown-item">Pas de catégorie</a>
                    {% endfor %}
                </div>
            </div>
            <div class="dropdown ml-auto">
                <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Marques
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton2">
                    {% for brand in brands %}
                        <a class="dropdown-item" href="/brand/{{ brand.id }}">{{ brand.name }}</a>
                    {% else %}
                        <a class="dropdown-item">Pas de marques</a>
                    {% endfor %}
                </div>
            </div>
            <ul class="navbar-nav text-uppercase  ml-auto">
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="/contact">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>